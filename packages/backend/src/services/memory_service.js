/**
 * @file memory_service.js
 * @description 本地记忆服务入口文件，加载并导出记忆服务主模块，
 *              支持六维状态建模、异步记忆结算、防模式坍缩和多用户支持
 * @module services/memory_service
 * @author xiaomeng
 * @version 1.0.0
 * @date 2026-06-07
 *
 * 核心理念：
 * - 关系是动态变量，每轮对话都会变化
 * - 六维状态建模：环境、时间、记忆、情感、话题、关系
 * - 异步记忆结算：对话后静默提炼关键信息
 * - 防模式坍缩：记录回复风格，避免重复表达
 * - 多用户支持：工厂模式 + 实例缓存
 *
 * 架构：
 * ┌──────────────────────────────────────────┐
 * │            MemoryService                  │
 * │  工厂函数 + 实例管理 + 状态同步             │
 * └──────────────────────────────────────────┘
 *                    ↓
 *           ┌───────────────────────┐
 *           │     子模块拆分         │
 *           ├───────────────────────┤
 *           │ main.js             │ ← 入口、工厂函数
 *           │ state_manager.js   │ ← 六维状态管理
 *           │ stores/            │ ← 存储层
 *           │   ├── episodic_store.js   ← 情景记忆
 *           │   └── semantic_store.js  ← 语义记忆
 *           │ relationship_manager.js  ← 关系管理
 *           └───────────────────────┘
 */

// ============================================================
// 模块名称：本地记忆服务入口
// 功能说明：加载并导出记忆服务主模块
// ============================================================

const MemoryServiceMain = require('./memory_service/main');

// 导出记忆服务主模块
module.exports = MemoryServiceMain;